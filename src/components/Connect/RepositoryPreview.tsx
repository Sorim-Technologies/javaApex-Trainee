import React from "react";

export function RepositoryPreview() {
  return null; // Under the current requirements, Step 1 does not need to render a persistent repository preview card since we transit to Step 2 immediately.
}

export default RepositoryPreview;
